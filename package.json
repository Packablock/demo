{
  "name": "packablock-demo-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:typescript": "bun --cwd packages/typescript-service run src/index.ts",
    "run:python": "python3 packages/python-processor/main.py",
    "docs:serve": "mkdocs serve",
    "docs:build": "mkdocs build"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
